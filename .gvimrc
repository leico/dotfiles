

if has('gui_macvim')

	"Color Setting
	colorscheme desert 
	"set transparency=15

	set guifont=Ricty:h14

	"Toolbar Hide
	set guioptions-=T

	"Tab Bar always Show
	set showtabline=2

	"Window Size
	set lines=52
	set columns=180

	"BugFix MacVim-KaoriYa Vim-LaTeX
	set imdisable

endif
